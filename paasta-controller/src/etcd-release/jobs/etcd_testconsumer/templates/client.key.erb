<%=
  key = nil
  respond_to?(:if_link) && if_link("etcd") do |link|
    if link.p("etcd.require_ssl")
        key = link.p("etcd.client_key")
    end
  end.else do
    if p("etcd_testconsumer.etcd.require_ssl")
        key = p("etcd.client_key")
    end
  end
  key
%>
